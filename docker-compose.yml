version: '3'
services:
  postgres-db:
    container_name: container-postgres-db
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: oyvind
      POSTGRES_PASSWORD: oyvind
      POSTGRES_DB: oyvind-db1